CAPI=2:
# Copyright lowRISC contributors (OpenTitan project).
# Licensed under the Apache License, Version 2.0, see LICENSE for details.
# SPDX-License-Identifier: Apache-2.0
name: "lowrisc:fpv:spi_device_fpv:0.1"
description: "spi_device FPV target"
filesets:
  files_formal:
    depend:
      - lowrisc:prim:all
      - lowrisc:ip:spi_device:0.1
      # TODO: add more dependencies here if needed
    files:
      - vip/spi_device_assert_fpv.sv
      - tb/spi_device_bind_fpv.sv
      - tb/spi_device_tb.sv
    file_type: systemVerilogSource


targets:
  default: &default_target
    # note, this setting is just used
    # to generate a file list for jg
    default_tool: icarus
    filesets:
      - files_formal
    toplevel: spi_device_tb

  formal:
    <<: *default_target

  lint:
    <<: *default_target
